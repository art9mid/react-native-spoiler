///
/// HybridNitroSpoilerComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroSpoilerComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroSpoilerSpecSwift.hpp"
#import "NitroSpoiler-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::spoiler;
using namespace margelo::nitro::spoiler::views;

/**
 * Represents the React Native View holder for the Nitro "NitroSpoiler" HybridView.
 */
@interface HybridNitroSpoilerComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroSpoilerComponent {
  std::shared_ptr<HybridNitroSpoilerSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroSpoilerComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroSpoilerComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridNitroSpoilerSpec> hybridView = NitroSpoiler::NitroSpoilerAutolinking::createNitroSpoiler();
    _hybridView = std::dynamic_pointer_cast<HybridNitroSpoilerSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroSpoiler::HybridNitroSpoilerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridNitroSpoilerProps const>(props);
  auto& newViewProps = const_cast<HybridNitroSpoilerProps&>(newViewPropsConst);
  NitroSpoiler::HybridNitroSpoilerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // isOn: boolean
  if (newViewProps.isOn.isDirty) {
    swiftPart.setIsOn(newViewProps.isOn.value);
    newViewProps.isOn.isDirty = false;
  }
  // color: optional
  if (newViewProps.color.isDirty) {
    swiftPart.setColor(newViewProps.color.value);
    newViewProps.color.isDirty = false;
  }
  // managesContentAlpha: optional
  if (newViewProps.managesContentAlpha.isDirty) {
    swiftPart.setManagesContentAlpha(newViewProps.managesContentAlpha.value);
    newViewProps.managesContentAlpha.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroSpoiler::NitroSpoilerAutolinking::isNitroSpoilerRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroSpoiler::HybridNitroSpoilerSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroSpoiler::HybridNitroSpoilerSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
