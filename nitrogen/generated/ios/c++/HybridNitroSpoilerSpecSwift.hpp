///
/// HybridNitroSpoilerSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridNitroSpoilerSpec.hpp"

// Forward declaration of `HybridNitroSpoilerSpec_cxx` to properly resolve imports.
namespace NitroSpoiler { class HybridNitroSpoilerSpec_cxx; }



#include <string>
#include <optional>

#include "NitroSpoiler-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::spoiler {

  /**
   * The C++ part of HybridNitroSpoilerSpec_cxx.swift.
   *
   * HybridNitroSpoilerSpecSwift (C++) accesses HybridNitroSpoilerSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridNitroSpoilerSpec_cxx can directly inherit from the C++ class HybridNitroSpoilerSpec
   * to simplify the whole structure and memory management.
   */
  class HybridNitroSpoilerSpecSwift: public virtual HybridNitroSpoilerSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridNitroSpoilerSpecSwift(const NitroSpoiler::HybridNitroSpoilerSpec_cxx& swiftPart):
      HybridObject(HybridNitroSpoilerSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline NitroSpoiler::HybridNitroSpoilerSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridNitroSpoilerSpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    inline bool getIsOn() noexcept override {
      return _swiftPart.isOn();
    }
    inline void setIsOn(bool isOn) noexcept override {
      _swiftPart.setIsOn(std::forward<decltype(isOn)>(isOn));
    }
    inline std::optional<std::string> getColor() noexcept override {
      auto __result = _swiftPart.getColor();
      return __result;
    }
    inline void setColor(const std::optional<std::string>& color) noexcept override {
      _swiftPart.setColor(color);
    }
    inline std::optional<bool> getManagesContentAlpha() noexcept override {
      auto __result = _swiftPart.getManagesContentAlpha();
      return __result;
    }
    inline void setManagesContentAlpha(std::optional<bool> managesContentAlpha) noexcept override {
      _swiftPart.setManagesContentAlpha(managesContentAlpha);
    }

  public:
    // Methods
    inline bool reveal(double x, double y) override {
      auto __result = _swiftPart.reveal(std::forward<decltype(x)>(x), std::forward<decltype(y)>(y));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline void touch(double x, double y) override {
      auto __result = _swiftPart.touch(std::forward<decltype(x)>(x), std::forward<decltype(y)>(y));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void release() override {
      auto __result = _swiftPart.release();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }

  private:
    NitroSpoiler::HybridNitroSpoilerSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::spoiler
